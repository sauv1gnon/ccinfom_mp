{
  "mcpServers": {
    "filesystem": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "--mount",
        "type=bind,src=D:/Work/Products/ccinfom_mp,dst=/projects",
        "mcp/filesystem",
        "/projects"
      ]
    }
  }
}